import { EscapeEntityHtmlPipePipe } from './escape-entity-html-pipe.pipe';

describe('EscapeEntityHtmlPipePipe', () => {
  it('create an instance', () => {
    const pipe = new EscapeEntityHtmlPipePipe();
    expect(pipe).toBeTruthy();
  });
});
